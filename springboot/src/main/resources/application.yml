server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/springboot
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true